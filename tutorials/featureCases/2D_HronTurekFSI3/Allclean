#!/bin/bash

# Source tutorial clean functions
. $WM_PROJECT_DIR/bin/tools/CleanFunctions

# Clean the case
foamListTimes -rm # Remove all time directories
rm -r postProcessing # Remove post-processing directories
rm -r dynamicCode # Remove dynamic code directories
rm log.* # Remove log files
rm yDispComp.png # Remove displacement component image
rm xDispComp.png # Remove displacement component image
rm displacementHistory.dat # Remove displacement component image
