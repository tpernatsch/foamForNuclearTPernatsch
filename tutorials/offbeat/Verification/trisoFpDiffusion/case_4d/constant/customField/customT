#include "yourCustomField.H"
#include "Pstream.H"

void customT::update()
{
    const scalar t = runTime.timeName();
    scalarField& Tfield = mesh().lookupObject<scalarField>("T");
    if (t <= 4.32e7)
    {
      Tfield = 1273.15;
    }
    else
    {
      Tfield = 1873.15;
    }
}
