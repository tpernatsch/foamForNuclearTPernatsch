description: |-
  This boundary condition provides a simplified black body radiation condition
  calculated as:

  $$
      T_{mat} = \sqrt[4]{\frac{q''}{\sigma \epsilon} + T_a^4}
  $$

  The heat flux is approximated using the Fourier law with the cells at the
  boundary:

  $$
      q'' = \kappa \frac{T_{cell} - T_{patch face}}{d}
  $$



options:
  - key: Ta
    type: scalar
    required: true
    default:
    description: Ambient temperature
  - key: emissivity
    type: scalar
    required: true
    default:
    description: Emissivity
  - key: kappa
    type: scalar
    required: true
    default:
    description: Conductivity of the material being simulated

usage:
  - comment: |-
      Example of the boundary condition specification:
    snippet: |-
      <patchName>
      {
          type            blackBodyRadiation;
          emissivity      0.78;
          kappa           10;
          Ta              10;
          value           uniform 300; // Surface temperature
      }
