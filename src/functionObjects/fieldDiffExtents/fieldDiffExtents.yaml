description: |-
  Calculates the spatial minimum and maximum extents of a difference between
  two fields.

  The extents are derived from the bound box limits after identifying the
  locations where one field exceeds the maskField

  See also
      Foam::functionObjects::fvMeshFunctionObject
      Foam::functionObjects::writeFile

admonitions:
  - kind: warning
    body: |-
      Preliminary autogenerated documentation. Validate the description, options, and usage before publishing.
  - kind: note
    body: |-
      For non-scalar fields, the magnitude of the field is employed and compared
      to the mask field magnitude

options:
  - key: fields
    type: word
    required: true
    default:
    description: |
  - key: maskFields
    type: word
    required: true
    default:
    description: |

usage:
  - comment: |-
      Usage already present in Header (verbatim):
    snippet: |-
      // Example of function object specification:
      fieldDiffExtents1
      {
          type        fieldDiffExtents;
          libs        ("libFFSSrc.so");
          ...
          writeToFile yes;
          log         yes;
          fields      (T.structure);
          maskFields  (T.interface);
          patches     ();
      }

  - comment: |-
      The functionObject is specified via the ``type`` entry in the ``functionObjects`` subdictionary of the ``controlDict`` file (located in ``system/``. Here is a snippet enabling the functionObject:
    snippet: |
      functionObjects
      {
          <functionObject1>
          {
              type fieldDiffExtents;
          }
      }
