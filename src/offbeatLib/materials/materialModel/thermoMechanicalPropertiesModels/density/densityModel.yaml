description: |
  Mother class for density models.

admonitions:
  - kind: warning
    body: |
      The keyword `densityModel` is deprecated. Prefer an `density` dictionary with a `type` entry.

options: []

usage: []