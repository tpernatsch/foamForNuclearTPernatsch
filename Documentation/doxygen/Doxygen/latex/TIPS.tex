
\begin{DoxyItemize}
\item When unsure about the functionalities of a class, take a look at the corresponding header file in the source code. In most case you\textquotesingle{}ll find useful information.
\item When unsure about the meaning of an input parameters, take a look at the tutorials. Many of them are commented. Another option is the famous \char`\"{}banana method\char`\"{}. Type in anything (for instance \char`\"{}banana\char`\"{}). In many cases, the solver will tell you that \char`\"{}banana\char`\"{} is not a valid option, and it will suggest the valid options.
\item default\+Prec has 1/m3 units except for the adjoint solver that needs 1/m2/s.
\item In point kinetics, p\+Target (in reactor\+State) M\+U\+ST be the same as the one used for reaching the steady-\/state. As power, Ge\+N-\/\+Foam uses what it finds under power\+Density, or under the power\+Density of the fluid\+Region if it does not find a power\+Density in the neutro\+Region. p\+Target does not enter the calculation, it is used simply to plot the results 
\end{DoxyItemize}